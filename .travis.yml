{
  "language": "minimal",
  "os": [
    "linux"
  ],
  "dist": "xenial",
  "deploy": {
    "provider": "pages",
    "strategy": "git",
    "skip_cleanup": true,
    "keep_history": true,
    "on": {
      "branch": [
        "preprod"
      ]
    },
    "token": "$GITHUB_TOKEN",
    "local_dir": ., 
    "target_branch": "preprod"
  }
}
